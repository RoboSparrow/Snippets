```javascript
   // ... package.json
  "scripts": {
    "test": "./node_modules/.bin/mocha --require babel-core/register --reporter spec tests || true",
  },
  // ...etc
  ``
  
  The `|| true` avoids the long npm error trace when a test fails and mocha exits
