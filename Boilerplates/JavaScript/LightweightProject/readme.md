# Install

* edit `package.json` for version, author, license etc
* add info, dependencies to `bower.json`

```
(sudo) npm install
bower install
```

# Development

## build/publish

```!bash
#Build
grunt

#watch
grunt watch
```
